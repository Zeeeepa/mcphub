{
  "name": "mcphub-cloudflare-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for MCPhub",
  "main": "src/index.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "publish": "wrangler publish"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

