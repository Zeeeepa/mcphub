{
  "name": "mcphub-cloudflare-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for MCPhub MCP protocol",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "test": "wrangler dev --test-scheduled",
    "format": "prettier --write .",
    "lint": "eslint src/"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "mcp",
    "mcphub",
    "proxy"
  ],
  "author": "MCPhub Team",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240129.0",
    "wrangler": "^3.28.2",
    "prettier": "^3.2.5",
    "eslint": "^8.56.0"
  },
  "dependencies": {}
}

